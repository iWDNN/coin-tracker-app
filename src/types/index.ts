export interface IPopUp {
  iconTag: string;
  textList: string[];
}
export interface ISignal {
  color: string;
  size: number;
}

export interface ICrypto {
  id: string;
  is_active: boolean;
  is_new: boolean;
  name: string;
  symbol: string;
  type: string;
}
